# Dynamics 365 General Journal Data Integration Pipeline

This automation toolkit executes financial transactional validation and layout compilation matching rules specified under document reference protocol `Zoho Payment_D365_Automation_Rules.docx`.

## Operational Prerequisites
Ensure dependencies are satisfied locally inside a sandboxed Python virtual execution node environment:

```bash
pip install -r requirements.txt
